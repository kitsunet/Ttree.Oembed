prototype(Ttree.Oembed:Resource) < prototype(TYPO3.TypoScript:Template)
prototype(Ttree.Oembed:Resource) {
    templatePath = 'resource://Ttree.Oembed/Private/Templates/TypoScript/Resource.html'
    uri = ${q(node).property('uri')}
    maxWidth = ${q(node).property('maxWidth')}
    maxHeight = ${q(node).property('maxHeight')}
}